#include <iostream>

int MulThenSub(int a_param, int a_param2)
{
	if (a_param % 2 ==0)
	return a_param * a_ param2 - a_param2;

}
void printNum(int n)
{
	printf("%d", n);
}
int scanNum()
{
	int var = 0;
	scanf_s("%d", &var);
	getchar();
	return var;
}

int main()
{
	int result = MulTHenSub((MulThenSub(scanNum(), scanNum());
	printNum(result);
	getchar();
}